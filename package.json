{
  "name": "xomyak",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Three.js 3D-симулятор хомяков с вращением камеры, уходом и автономным поведением",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check bootstrap.js && node --check app.js && node --check animation.js && node --check scene.js && node --check furniture.js && node --check hamster.js && node --check geometry.js && node --check materials.js && node --check layout.js && node --check navigation.js && node --check engine.js"
  },
  "engines": {
    "node": ">=20"
  }
}
